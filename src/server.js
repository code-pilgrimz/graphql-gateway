const app = require("./app");
const port = process.env.PORT || 3000;
app.listen(port, () => console.log(`up on ${port}`));
// TODO clean this
// check perf here
// TODO clean this
// off-by-one, fixed
// off-by-one, fixed
// minor wording
